const screen = {
    width: 1024,
    height: 768
};

const PRE_LOADER_TIMEOUT = 5000;
const ANIMATION_TIMEOUT = 5000;
const REDIRECT_TIMEOUT = 10000;

module.exports = {
    screen,
    PRE_LOADER_TIMEOUT,
    ANIMATION_TIMEOUT,
    REDIRECT_TIMEOUT,
};